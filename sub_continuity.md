- 2026-01-29: Completed MarketData/Realtime ingestion reliability audit (MarketDataAgent, PolymarketRealtime, PolymarketClob, orderbook). Key findings: missing sequence/gap detection; REST snapshot can be rejected as out-of-sequence; WS book normalization drops tick/min_order_size; heartbeat does not count pongs (false stale); float price equality can duplicate levels; no reconnect resync/mark-stale handling; REST empty body not treated as error. Suggested fixes recorded in response.
- 2026-01-29: Completed config/policy clarity + risk gate reliability audit (no code changes). Key findings: orderbookFreshnessMs is effectively dead because maxBookStalenessMs always set; clarify/merge or make one optional. Clarify semantics for zero values in risk/policy (e.g., maxDailyDrawdownFraction=0 currently blocks trading when dailyPnL<=0; depthBufferMultiplier=0 allowed in code but disallowed in schema; 0 for timeouts ambiguous). RiskAgent drawdown check should guard >0 to avoid unintended block. Consider validating inter-field constraints (targetTradeFraction <= maxTradeFraction; edgeRequired < maxEdge already enforced) and documenting/validating which limits can be disabled. Gate pricing validity: orderbook normalize allows negative/zero prices; gates should explicitly reject bestAsk <=0 or >1 for clearer failure reasons. Document that orderbookFreshnessMs vs maxBookStalenessMs differ or remove redundant field.

## 2026-01-29 - OpenCode Zen model ID scan
- Scanned repo for OpenCode Zen model IDs in env defaults, mappings, tests.
- Compared against OpenCode Zen /models list; grok-code appears missing from live list while other IDs (minimax-m2.1-free, glm-4.7-free, gpt-5-nano, qwen3-coder) are present.
- Suggested updates (not applied): replace grok-code defaults/backups/mappings/tests with a current chat model (e.g., qwen3-coder or glm-4.7-free) and adjust OpenRouter↔Zen mapping/tests accordingly.

- 2026-01-29: Focused audit (market data, execution, risk gates). Findings: refreshSnapshot errors can surface as unhandled rejections in MarketDataAgent; depth/staleness gating uses lastUpdateMs even for delta-only updates so depth can be stale; crossed books are not explicitly rejected because spread() clamps negatives to 0; ExecutionAgent falls back to cancelMarketOrders when orderId unknown (can cancel unrelated orders); non-near-zero mode marks fills without confirmation; gates use gross edge only (fees ignored). Proposed fixes captured in response.
- 2026-01-29: Reliability/policy audit (price_change, gap detection, pong keepalive, gates). Findings: gap detection still partial (SequenceTracker unused; best_bid/ask mismatch only when fields present), out-of-sequence uses raw timestamps without ms/sec normalization; pong keepalive updates on pong but not ping (server-driven ping could still trigger stale reconnects); crossed-book spread is clamped to 0 so gates may pass invalid books; requireFreshBook only skips staleness check (leg skew + stability still enforced). Proposed fixes documented in response.
- 2026-01-30: Reviewed ops alert flow + book_freshness check for stale-market quarantine wiring. Findings: OpsAgent emits ops:alert + metrics incident on failing checks but never routes to IncidentTracker/allowlist; book_freshness check only returns worst tokenId in info string (no marketId); IncidentTracker quarantine only occurs when called by Execution/Portfolio. Recommended wiring: add an ops:alert handler near ops check setup (or in Supervisor) that computes stale tokens using shared freshness logic, maps tokenId→marketId via marketPairs, tracks repeated failures per market in a rolling window, and calls IncidentTracker.record with reason book_stale when threshold reached; avoid quarantines on no_orderbooks/no_active_orderbooks (treat as global feed issue).
- 2026-01-30: Inspected risk gate logic and profiles (`src/domain/gates.ts`, `src/config/policy.ts`, `src/config/risk.ts`, `settings/risk-gates/*.json`). Gate rejection reasons are the `GateDecision.reasons` strings; `evaluateGates` appends each failed check and passes only when reasons is empty (early return on missing best ask or invalid best ask), while `evaluateGatesWithFees` returns base reasons unless base passed, then adds `edge_below_threshold_after_fees`/`edge_above_max_after_fees` based on net edge. Restrictive checks include: best ask validity (0 < price <= 1), crossed book (bid > ask), missing spread, spread > `maxSpread`, edge < `edgeRequired`, edge > `maxEdge`, staleness > `maxBookStalenessMs` (if `requireFreshBook`), leg skew > `maxLegSkewMs`, unstable top of book (`stableSinceMs` < `topOfBookStabilityMs`), tick misalignment, depth limits (`minDepthLevels`, `depthHeadroomFraction`, `below_min_order_size`, `desired_size_exceeds_depth`, `depthBufferMultiplier`), min edge ticks (`minEdgeTicks` using resolved tick size = max of input/book/fallback), depth exhaustion, and slippage > `entrySlippageToleranceBps`. Defaults: `DEFAULT_TRADE_POLICY` matches `near_zero.json` (edgeRequired 0.03, maxEdge 0.05, maxSpread 0.05, depthHeadroomFraction 0.25, topOfBookStabilityMs 250ms, maxLegSkewMs 100ms, minEdgeTicks 3, depthBufferMultiplier 1.5, entrySlippageToleranceBps 50, minDepthLevels 3, requireFreshBook true with maxBookStalenessMs 15000). Active profile per `settings/risk-gates/active.json` is `extra_high` (updatedAt 2026-01-17) with looser thresholds and `requireFreshBook: false`. Risk config defaults (and near_zero risk profile) can further cap sizing via `effectiveTradeFraction` and `calculateMaxSizeByUnwindBudget` (maxAttemptLossFraction 0.0025, maxUnwindLossFraction 0.025, maxUnwindLossTicks 2, maxPerTradeLossDollars 25, dailyLossLimitFraction 0.03), potentially reducing size to zero when capital or tick size is small.
- 2026-01-30: Reviewed MarketData + Ops/SLO + telemetry staleness/incident paths. Findings: book freshness gating is driven by OpsAgent `book_freshness` check (uses orderbook `lastUpdateMs` from MarketDataAgent/applyDelta, i.e., local receive time; exchangeTimestamp staleness is not considered). Any WS delta (price_change/best_bid_ask/tick_size_change) updates `lastUpdateMs`, so freshness reflects feed activity, not exchange age. `refreshStaleBooks` only logs info counts and never emits incidents/book_staleness events. OpsAgent emits `ops:alert` + `incident` metrics for any failing check, but `bookFreshnessQuarantine` only acts when `result.info` includes `worst=<tokenId> stalenessMs=...`; failures like `no_orderbooks`/`no_active_orderbooks` produce no info and therefore never quarantine. Quarantine also depends on `tokenToMarketId` mapping; missing or stale mappings skip quarantine. `idleCutoffMs` filters inactive books out of freshness checks, so inactive/stale books won’t drive violations. SLO aggregates count `book_freshness` incidents regardless of info, so SLO violations can include feed-dropout cases that do not quarantine.
- 2026-01-30: Researched probability estimation models + production best practices for trading systems. Summary: Logistic regression is a strong baseline for probability estimation (fast, interpretable), with predict_proba outputs; Bayesian logistic/probit models add priors and return posterior uncertainty (useful with sparse/noisy data, but higher compute). Ensembles (bagging/boosting/stacking) improve robustness/accuracy but often need post-hoc calibration. Time-series/state-space approaches (walk-forward CV, state-space + Kalman filtering) handle temporal dependence and latent dynamics but impose structure assumptions. Production best practices: (1) calibrate probabilities with held-out data (sigmoid/Platt or isotonic) and evaluate calibration via reliability diagrams + proper scoring rules (Brier/log loss); (2) separate temporal train/validation/test (walk-forward/TimeSeriesSplit) to avoid leakage; (3) monitor drift/skew in live features vs training baseline; (4) backtesting can overfit—use robust CV (e.g., CSCV/PBO framework) and keep a strict out-of-sample test; (5) retrain/recalibrate when drift or calibration error exceeds thresholds. Sources: scikit-learn calibration + TimeSeriesSplit docs; Stan regression guide; statsmodels state-space; Guo et al. (2017) on temperature scaling; Bailey et al. (2017) on PBO; Google Vertex AI/AWS Model Monitor drift docs.
- 2026-01-30: Researched Polymarket CLOB constraints. Orderbook summary `/book` returns market/asset_id/timestamp/hash plus bids/asks levels (price, size) and per-market `min_order_size` + `tick_size` + `neg_risk`; changelog confirms tick_size/min_order_size were added to get-book/get-books on 2025-07-23. Market WS `book` messages include bids/asks, timestamp, hash; `tick_size_change` fires when price goes >0.96 or <0.04, so tick size can change intra-market. Gamma markets API exposes `orderPriceMinTickSize` and `orderMinSize` for discovery/validation. Market-makers trading docs list tick sizes as 0.1/0.01/0.001/0.0001. Fees: CLOB intro + trading docs list maker/taker base rate 0 bps, but Maker Rebates Program docs state taker fees are enabled on 15-minute crypto markets; `/fee-rate?token_id=...` returns `fee_rate_bps` (0 or 1000) and orders without correct `feeRateBps` are rejected. Fee curve uses feeRate=0.25 and exponent=2 with min fee precision 0.0001 USDC. Impact on EV/execution: snap prices to tick size and recompute edge after rounding; enforce min_order_size per leg (and per child order if splitting) or you get rejections; compute expected fill using depth (bids/asks sizes) and timestamp/hash to avoid stale/slippage overestimation; incorporate taker fee curve (max at p=0.5) and only treat maker rebates as optional upside; always fetch fee_rate_bps per token and react to tick_size_change. Sources: docs.polymarket.com/api-reference/orderbook/get-order-book-summary; docs.polymarket.com/changelog; polymarket-292d1b1b.mintlify.app/developers/CLOB/websocket/market-channel; docs.polymarket.com/developers/gamma-markets-api/get-markets; docs.polymarket.com/developers/market-makers/trading; docs.polymarket.com/developers/market-makers/maker-rebates-program; docs.polymarket.com/developers/CLOB/introduction.
- 2026-01-30: Researched best practices for probability estimation/calibration in prediction markets. Key sources: Gneiting, Balabdaoui & Raftery (2007 JRSSB) on calibration/sharpness + proper scoring rules; "Forecasting: theory and practice" (online textbook) on calibration definition and PIT diagnostics; Wolfers & Zitzewitz (2006 FRBSF working paper) on prices approximating mean beliefs with risk aversion/belief dispersion; Manski (2004 NBER working paper) on risk-neutral price-taker interpretation and limits; Hanson (2007 Journal of Prediction Markets) on market scoring rules. Summary: evaluate with strictly proper scoring rules (e.g., log/CRPS/Brier) and track calibration via PIT/reliability; target sharpness subject to calibration; interpret prices as average beliefs only under assumptions and expect bias when risk aversion or belief dispersion is large; market scoring rules can combine truthful elicitation with market aggregation.
- 2026-01-30: Researched news sentiment + event extraction sources for forecasting. Key takeaways: GDELT Cloud offers 30-day rolling events/GKG updated every 15 minutes and is open, but ONS data-quality review flags non-robustness, Western/US media bias, and limited QA/transparency. EventRegistry/NewsAPI.ai provides 150k+ sources in 60+ languages, event clustering, sentiment + full content, pay-per-use, and archive access since 2014. NewsAPI.org free plan is dev-only with 24h delay, 1-month history, 100 req/day, and no full-article text; terms prohibit production use on the free plan and restrict republishing. Open/other datasets: Media Cloud Online News Archive (200M+ stories) offers API/search but cannot share full text and has quotas; Common Crawl News dataset is WARC-based and early-stage (requires extraction/dedup); GTD is open-source terrorist event data (1970–2020) with public access; ICEWS data is hosted on Harvard Dataverse (historic + weekly DOIs) but discontinued in 2023; ACLED terms prohibit ML/LLM training that substitutes for ACLED. Best practices: triangulate across sources to mitigate bias/quality issues, use event-level clustering/deduplication, align history windows to forecast horizon, and verify licensing before model training.
- 2026-01-30: Researched social sentiment sources for forecasting markets (X/Twitter, Reddit, YouTube, Google Trends, Wikipedia pageviews) and pitfalls (sampling/normalization, demographic bias, social bots/manipulation, platform drift). Key sources: https://www.sciencedirect.com/science/article/pii/S187775031100007X (Twitter mood + DJIA), https://aclanthology.org/2021.finnlp-1.4/ (Reddit sentiment + GME), https://arxiv.org/abs/2311.15247 (YouTube channel sentiment + market timing), https://www.nature.com/articles/srep01684 (Google Trends + trading signals), https://support.google.com/trends/answer/4365533 (Trends normalization/sampling caveats), https://www.nature.com/articles/srep01801 (Wikipedia pageviews + stock moves), https://doc.wikimedia.org/generated-data-platform/aqs/analytics-api/ (pageviews API), https://doaj.org/article/8dbd61f324ba4469b19e61666850458a (social media demographic bias), https://www.nature.com/articles/s41467-018-06930-7 (social bots amplification), https://www.uh.edu/news-events/stories/2014/March/03132014GoogleFlu.php (platform drift/big-data pitfalls).
- 2026-01-30: Researched probability estimation best practices for event prediction (calibration, proper scoring rules, time-based CV). Key findings: strictly proper scoring rules (e.g., logarithmic, quadratic/Brier) are designed so truthful probabilistic forecasts maximize expected score; proper scoring rules provide evaluation incentives and examples include log and quadratic scores (Gneiting & Raftery 2007 JASA). Calibration is statistical consistency between forecast distributions and observations; sharpness is concentration of predictive distributions; recommended evaluation emphasizes maximizing sharpness subject to calibration (Gneiting, Balabdaoui & Raftery 2007 JRSSB). Calibration curves/reliability diagrams assess calibration by binning predicted probabilities and comparing predicted vs observed frequencies; proper scoring rules like Brier/log loss combine calibration (reliability) and discrimination (resolution), so they are not pure calibration measures (scikit-learn calibration docs; Murphy decomposition noted). Time series CV should use rolling-origin splits: each test set uses only prior observations, average errors across sequential test points, and can be adapted for multi-step horizons (Hyndman & Athanasopoulos FPP3). Sources: https://sites.stat.washington.edu/raftery/Research/PDF/Gneiting2007jasa.pdf ; https://sites.stat.washington.edu/raftery/Research/PDF/Gneiting2007jrssb.pdf ; https://scikit-learn.org/stable/modules/calibration.html ; https://otexts.com/fpp3/tscv.html ; https://ui.adsabs.harvard.edu/abs/1950MWRv...78....1B/abstract
- 2026-01-30: Researched social/news/market sentiment signal sources + best practices for prediction markets. Source constraints/availability: X API access is tiered with paid plans and rate limits, so ingestion cost/throughput must be budgeted and backfilled via approved products (no scraping). citeturn1search1 Reddit API access is allowed via OAuth with explicit rate limits and requires honoring user deletions, which affects backfill and storage retention. citeturn0search3 YouTube Data API is quota-based (per-project daily units) and requires careful request budgeting or batching; video/comment retrieval can be expensive. citeturn0search2 GDELT Cloud provides a 30-day rolling events/GKG dataset updated every 15 minutes (good for near-real-time signals but limited historical depth unless you store it). citeturn1search0 NewsAPI free plan is dev-only with 24-hour delay, 1-month history, 100 req/day, and terms prohibit production use without a paid plan (impacts live trading use). citeturn0search0 Google Trends data is normalized and sampled, so absolute levels are not available and repeated queries can vary; use consistent geo/time windows and re-sampling/averaging. citeturn0search1 Wikipedia pageviews are available via the Wikimedia Pageviews API for time-series traffic features. citeturn0search5
- Best-practice takeaways: (1) Treat social/news signals as noisy and biased; demographic skew and platform biases are documented, so use source-mix and robustness checks. citeturn1search2turn1search4 (2) Guard against manipulation/bot amplification on social platforms; apply bot/spam filters and credibility weighting. citeturn1search4 (3) Run lead-lag tests and walk-forward evaluation to ensure signals add predictive power without lookahead; re-validate after major platform or policy shifts (platform drift risk is real). citeturn1search4turn1search5 (4) Deduplicate and cluster news/event sources to avoid double-counting the same event; prioritize event-level features over raw story counts where possible. citeturn1search0turn0search4 (5) Normalize query-based signals (Trends, pageviews) with stable baselines and align to contract horizons (e.g., 7d/30d outcomes) to avoid horizon mismatch. citeturn0search1turn0search5

- 2026-01-30: Researched Polymarket per-market min order size/tick size and prefetch options. CLOB `GET /book` orderbook summary includes per-market `min_order_size` and `tick_size` fields in the response, so min order size is provided directly per token; changelog confirms these fields were added to `get-book`/`get-books` on 2025-07-23. citeturn0search2turn0search1 Prefetch approach: call `GET /book` (or `get-books` for batch) per token to load `min_order_size`/`tick_size` for trading decisions, and listen for `tick_size_change` events on the Market WebSocket to update cached tick sizes when price approaches extremes. citeturn0search2turn0search0 Alternative discovery: Gamma markets API includes `orderMinSize` and `orderPriceMinTickSize` fields for markets, useful for prefetch/validation alongside CLOB. citeturn0search5

2026-01-30 - Research: Prediction market probability estimation + calibration + combining priors
- Market prices as probabilities: theory shows binary market prices are typically close to mean beliefs under reasonable conditions, though can be biased with risk aversion/heterogeneous beliefs; use as probabilistic priors not gospel. Source: Wolfers & Zitzewitz, "Interpreting Prediction Market Prices as Probabilities" (IZA DP 2092). https://www.iza.org/publications/dp/2092/interpreting-prediction-market-prices-as-probabilities
- Prediction markets aggregate dispersed info and tend to be accurate vs benchmarks; treat market price as strong baseline/prior EV signal. Source: Wolfers & Zitzewitz, "Prediction Markets" (NBER Working Paper 10504). https://www.nber.org/papers/w10504
- Market scoring rules (LMSR/logarithmic) connect markets to proper scoring; log scoring preserves conditional probabilities and supports coherent aggregation. Source: Hanson, "Logarithmic Market Scoring Rules..." Journal of Prediction Markets (2007). https://www.ubplj.org/index.php/jpm/article/view/417
- Calibration best practice: evaluate probabilistic forecasts with strictly proper scoring rules (e.g., log score, quadratic/Brier); proper scoring incentivizes honest probabilities and aligns with forecast quality. Source: Gneiting & Raftery, "Strictly Proper Scoring Rules, Prediction, and Estimation" (UW tech report). https://stat.uw.edu/research/preprints/tech-report/strictly-proper-scoring-rules-prediction-and-estimation
- Calibration diagnostics and methods: reliability diagrams + Brier/log-loss; post-hoc calibration methods include Platt scaling, temperature scaling, isotonic regression; isotonic often optimized for Brier. Source: "Classifier calibration: a survey..." (Springer, 2023). https://link.springer.com/article/10.1007/s10994-023-06336-7
- Platt scaling details: fit sigmoid on model outputs to map scores to calibrated probabilities. Source: Platt, "Probabilistic Outputs for Support Vector Machines..." (1999). https://transferlab.ai/refs/platt_probabilistic_1999/
- Combining market priors with models: use forecast-combination/Bayesian model averaging ideas to blend market probability (prior) with model-based likelihood or forecast; weighted averages often outperform single methods. Sources: "Combination of Forecasts" (JRSS A). https://academic.oup.com/jrsssa/article/146/2/150/7105981 ; "Improving Predictions using Ensemble Bayesian Model Averaging" (Political Analysis). https://www.cambridge.org/core/journals/political-analysis/article/improving-predictions-using-ensemble-bayesian-model-averaging/11866974EE2888D4A2988309FC6B602F
- 2026-01-30: Reviewed EV single-sided execution integration points (ExecutionAgent + domain execution/opportunity). Required changes (ExecutionAgent-focused): branch on `opportunity.type === 'ev'` to run a single-leg flow; derive idempotency keys from `opportunity.id` (or include `side` + price) to avoid collisions with same market/time; compute `plannedOrders` from actual legs (1 for EV) so OTR/velocity checks and `recordOrderAttempt`/`recordFill` counts match; price-band validation must check only the traded side (don’t require both books; otherwise EV can skip price validation); only `portfolio.expectFill` for the traded token; build one FOK order payload using `opportunity.side` to pick token/price; skip paired-leg cancel/unwind logic (single-leg failures should cancel only that order; no unwind); adjust fill-wait logic to wait for just the EV order; and either extend `ExecutionState` or add a single-leg state type so `COMPLETE` doesn’t require both legs. Consider adding `opportunity.type`/`side` into execution lifecycle metrics/outcomes for EV tracking. Pitfalls: current paired state machine cannot reach `complete` with one leg; `plannedOrders=2` over-throttles EV via OTR/velocity; price-band check currently ignores single-side updates unless both books provided; EV would erroneously trigger unwind logic on partial fill; idempotency keys collide across EV signals if side/price not included; fallback `cancelMarketOrders` can cancel unrelated orders; `rejectDelayed` hard-throw is unconditional (may need to scope to near-zero/paired); non-userRealtime path marks fills without confirmation (EV may need explicit fill tracking). Tests to update/add: `tests/unit/execution.test.ts` (new EV single-leg path, OTR/velocity counts=1, single-order cancel/failure, idempotency for EV); `tests/unit/execution-state.test.ts` (single-leg state machine or new transitions); `tests/unit/domain-utils.test.ts` if `evOpportunityId`/idempotency helpers become part of EV flow.
- 2026-01-30: Reviewed Supervisor/main wiring for SignalAggregatorAgent + EV gates. Findings: `src/main.ts` lacks websearch + SignalAggregator wiring; add `WebSearchCache`, `ExaClient`/`FirecrawlClient` instantiation from `EXA_*`/`FIRECRAWL_*`/`EV_WEBSEARCH_*` envs (incl. allow/deny list parsing), pass metrics, and inject `signalAggregator` into `Supervisor` deps. `Supervisor.updatePolicyAndRisk` already calls `signalAggregator.updatePolicy` (no change). `Supervisor.updateMarketPairs` should also refresh SignalAggregator’s market list when catalog refreshes pairs (requires new `SignalAggregatorAgent.updateMarketPairs` or re-init). `Supervisor.handleRiskApproved` currently uses `evaluateGates` for all opportunities; EV opportunities should gate via `evaluateEvGates` using `opportunity.side`, `opportunity.evNet` (or `edge`), and `opportunity.modelConfidence` to avoid incorrect rejections. Metrics: add `web_search` and `ev_signal` to `MetricEventType` + snapshot counts (used by SignalAggregator/Scanner); consider SSE/UI subscription for these types if visibility is desired.

- 2026-01-31: Reviewed EV-specific risk gating in RiskAgent/gates/risk config. Findings: EV gates lack maxEdge/minEdgeTicks/NaN guards; EV caps not surfaced in constraints and portfolio cap uses total exposure (confirm intent). No code changes.
- 2026-01-31: Reviewed SignalAggregator/websearch pipeline (EV) for security/perf risks. Findings: unbounded cache growth, provider cache key collisions, sequential blocking, Firecrawl crawl unbounded cost/latency.
- 2026-01-31: Reviewed risk gates in `src/domain/gates.ts` with supporting types in `src/domain/orderbook.ts` and policy thresholds in `src/config/policy.ts`. Base gates validate best asks, prevent crossed books, enforce `maxSpread`, staleness vs `maxBookStalenessMs`/`orderbookFreshnessMs`, leg skew (`maxLegSkewMs`), top-of-book stability (`topOfBookStabilityMs`), tick alignment, and depth at `minDepthLevels`. Near-zero gates add edge thresholds (`edgeRequired`/`maxEdge`), depth headroom (`depthHeadroomFraction`), min order size, depth buffer (`depthBufferMultiplier`), min edge ticks (`minEdgeTicks`), and slippage vs `entrySlippageToleranceBps`. EV gates add `evEdgeRequired` and `evConfidenceMin` plus side-specific depth/slippage checks. Gates filter trades in `src/agents/scanner/ScannerAgent.ts` (drop opportunities, emit `gate_rejection`/`ev_signal`) and `src/core/Supervisor.ts` (re-check with `desiredSize` before execution).
- 2026-01-31: Reviewed tests for risk gates/modes (gates, risk agent sizing, risk profile persistence/apply, near-zero-risk mode); documented coverage + gaps (no evaluateEvGates tests, limited integration of risk profile application).

- 2026-01-31: Summarized risk profiles/modes from config + risk agent files (near_zero/moderate/high/extra_high characteristics, defaults, and usage in boot/API/RiskAgent/strategy+signal+LLM advisor modes).

- 2026-01-31: Read `docs/EV_SIGNAL_REMEDIATION_PLAN.md`; summarized required changes and referenced files for EV remediation tasks (execution fill tracking, EV fees/gates, risk caps observability, websearch cache/concurrency, tests, docs).

- 2026-01-31: Scanned repo for EV signal pipeline components. Core files: src/agents/signal/SignalAggregatorAgent.ts (websearch + learning:insight), src/services/websearch/{WebSearchClient,WebSearchCache,ExaClient,FirecrawlClient}.ts, src/agents/scanner/ScannerAgent.ts (EV opportunity + evaluateEvGates), src/domain/gates.ts (evaluateEvGates), src/core/Supervisor.ts (EV gate recheck + signalAggregator lifecycle), src/agents/risk/RiskAgent.ts (evMax* caps), src/agents/execution/ExecutionAgent.ts (executeEvOrder), src/main.ts (websearch/signal wiring), src/config/{policy,env,schema,validate}.ts (EV knobs), src/telemetry/{metrics,events}.ts (ev_signal/web_search), src/domain/llm.ts (learning:insight schema). Likely tests to touch: tests/unit/ev_signal.test.ts, tests/unit/websearch.test.ts, tests/unit/telemetry.test.ts, tests/unit/gates.test.ts (no EV coverage), tests/unit/execution.test.ts (EV execution), tests/unit/risk.test.ts (EV caps), tests/unit/execution-llm-hints.test.ts and tests/unit/learning-agent.test.ts (learning:insight). Docs context: docs/EV_SIGNAL_SPEC.md, docs/EV_SIGNAL_REMEDIATION_PLAN.md, docs/EV_SIGNAL_IMPLEMENTATION_PLAN.md. Tool note: RepoPrompt MCP not available via list_mcp_resources.
- 2026-02-01: Reviewed EV cap observability in `src/agents/risk/RiskAgent.ts` and related types. Findings: EV caps (`evMaxPerMarketNotional`, `evMaxPortfolioNotional`) are enforced via `maxSizeByEvMarket`/`maxSizeByEvPortfolio` and `ev_notional_cap` rejection, but `RiskDecision.constraints` omits EV cap fields and `binding` excludes EV caps, so EV limits are not surfaced in approved decisions (nor passed to metrics/LLM). To make caps observable per Task 3, extend constraints with `maxByEvMarket`/`maxByEvPortfolio` and add binding variants (e.g., `ev_market`, `ev_portfolio`), then include them in `pickBindingConstraint` for EV opportunities; optionally surface which cap triggered for rejections (reason details or constraints even on reject).
- 2026-02-01: Reviewed Task 1 EV execution fill tracking in `src/agents/execution/ExecutionAgent.ts`. Findings: EV path skips user WS gating and fill wait. Changes needed: (1) set `requiresUserChannel` to include EV so `userRealtime` missing/disconnected blocks EV (around lines 907-932). (2) In `executeEvOrder`, after ACK + `orderId`, compute remaining fill timeout and call `waitForFillOutcome` for the EV order; on fully filled record `metrics.recordFill` and `recordLatency` for `filled`/`complete`, and update idempotency status to `confirmed`; on timeout/cancel/partial, `markIdempotencyFailed`, record incident, and return failed to avoid OTR inflation (lines ~1746-1823). (3) Guard missing `orderId` as failure (record incident) since fill wait cannot proceed. These align with EV_SIGNAL_REMEDIATION_PLAN Task 1 acceptance criteria.
- 2026-02-01: Reviewed EV_SIGNAL remediation Tasks 4–5 (websearch cache + concurrency). Findings: `src/services/websearch/WebSearchCache.ts` is unbounded (no max entries, no prune on insert); cache keys in `src/services/websearch/ExaClient.ts` + `src/services/websearch/FirecrawlClient.ts` are not provider-prefixed, so shared `WebSearchCache` can cross-contaminate search/content entries. Proposed: add max entry limits + prune expired/oldest on insert; prefix cache keys with provider for search + content. Noted existing concurrency helper `src/utils/concurrency.ts` (`mapWithConcurrency`)—reuse to avoid adding a new helper; add policy config `evWebSearchMaxConcurrency` and use it in `src/agents/signal/SignalAggregatorAgent.ts` runOnce + fetchSignals. Firecrawl crawl fallback currently unbounded; per Firecrawl v2 docs, add crawl caps via `limit` and `maxDiscoveryDepth` (plus optional `maxConcurrency`) in `src/services/websearch/FirecrawlClient.ts`, configured via new policy fields (e.g., `evWebSearchCrawlMaxPages`, `evWebSearchCrawlMaxDepth`) and validated in `src/config/{policy,schema,validate}.ts` + wired in `src/main.ts`. Tests: update `tests/unit/websearch.test.ts` or add `tests/unit/websearch_cache.test.ts` to cover TTL eviction and provider isolation.
- 2026-02-01: Coverage gap scan for `src/agents/execution/ExecutionAgent.ts` and `src/domain/gates.ts` (tests in `tests/unit/execution.test.ts`, `tests/unit/gates.test.ts`). Untested branches in ExecutionAgent: EV early rejects (`ev_missing_side`, `invalid_price`), EV delayed/rejected responses and missing `orderId`, circuit breaker open path, cancellation updates (`orderEventType=CANCELLATION` or status CANCELLED/CANCELED) and cancelled fill outcome, user trade update handling (taker + maker matches, duplicate trade IDs, invalid side/price paths), timeout outcome mapping in `emitExecutionOutcome`, and partial-fill via user channel where only one leg fills before timeout. Untested branches in gates: `requireFreshBook=false` (skip staleness), EV thresholds (`ev_edge_below_threshold`, `ev_confidence_below_min`), EV side=`no` slippage/depth labels, and `evaluateGatesWithFees` edgeInTicks fallback when all tick sizes invalid. Proposed tests: add targeted cases in `tests/unit/execution.test.ts` and `tests/unit/gates.test.ts` to hit these branches; optional minimal code tweaks for coverage include removing unreachable `if (!requiresUserChannel)` block in `executeEvOrder` and the duplicate `ev_missing_side` guard (already handled in `executeArbitrage`).

- 2026-02-01: Analyzed coverage gaps for `src/agents/signal/SignalAggregatorAgent.ts` (branch coverage ~60% per coverage-summary). Untested branches include: `start()` idempotency guard; `updatePolicy()` early return when not started; `applySchedule()` clearing existing timer + shouldRun false; `shouldRun()` when providers disabled or missing; `runOnce()` guard (inFlight/shouldRun false), pending=0 due to cache; `buildQueries()` empty question; `fetchSignals()` primary=firecrawl path, primary-success (no secondary), no-url results (skip fetchContents); `getMarketMeta()` cache hit + null/empty question path; per-market error handling + metrics record; recency/threshold edge cases for confidence/signal classification. Proposed concrete unit tests in `tests/unit/signal-aggregator.test.ts` to cover each branch (fake timers for TTLs, stub metrics, and optional module mock for runWithConcurrency error path).
- 2026-02-03: Summarized trading architecture/decision flow and decision-related config knobs from docs/ARCHITECTURE.md and docs/Operations/config-knobs.md; provided file references in response.
- 2026-02-03: Reviewed core trading flow (Supervisor, Scanner, Risk, gates, Execution, Portfolio) and summarized decision logic, inputs/outputs, and control points with file references for user.
- 2026-02-08: Reviewer-agent C audited backend AGENTS docs (root + src/* scope). Findings: stale file/test counts in AGENTS.md and src/AGENTS.md; learning agent description marked write-only despite learning:insight emission. No code changes.

- 2026-02-08: Reviewed ops/testing docs for correctness against code/config; reported findings (config-knobs, runbook, security) and noted files with no issues.
- 2026-02-08: Reviewer-agent D audited dashboard/tests/scripts AGENTS.md docs for correctness; reported findings and verified repo claims. (No code changes)
- 2026-02-08: Reviewer-agent A completed doc correctness review for README + architecture + setup + market catalog + ADRs + README image prompt files; reported mismatches with current scripts/configs (dev:live Docker scope, risk sizing/drawdown/edge thresholds, CLOB rate limit defaults, /metrics seeded count claim, Node version note, batch order claim).
